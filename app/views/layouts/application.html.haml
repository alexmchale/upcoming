!!!
%html

  %head

    %title Upcoming

    %script{src: "http://use.typekit.com/lfo2llh.js"}
    %script try{Typekit.load();}catch(e){}

    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags

  %body

    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          %ul.nav
            %li= image_tag "28-star.png", width: 26, height: 26
            %li= link_to "Upcoming", root_path, :class => "brand"
            %li{:class => "#{active_if 'searches#new'}"}= link_to "Search", new_search_path
            %li{:class => "#{active_if 'searches#index'}"}= link_to "Saved Searches", searches_path
            - if current_user
              %li= link_to "Sign Out", destroy_user_session_path, method: "delete"
            - else
              %li{:class => active_if('sessions#new')}= link_to "Sign In", new_user_session_path
              %li.sign-up{:class => active_if('registrations#new')}= link_to "Sign Up!", new_user_registration_path

    #main-content.container
      - if flash[:error].present?
        .alert.fade.in.alert-error
          %a.close{"data-dismiss" => "alert", :href => "#"} &times;
          = flash[:error]
      - if flash[:notice].present?
        .alert.fade.in.alert-notice
          %a.close{"data-dismiss" => "alert", :href => "#"} &times;
          = flash[:notice]
      - if flash[:alert].present?
        .alert.fade.in.alert-error
          %a.close{"data-dismiss" => "alert", :href => "#"} &times;
          = flash[:alert]
      = yield

    #main-footer.container
      Upcoming is a production of #{link_to "Anti Clever Software", "http://anticlever.com"}.
